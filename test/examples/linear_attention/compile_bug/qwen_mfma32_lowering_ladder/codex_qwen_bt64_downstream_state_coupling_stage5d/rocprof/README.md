# Pending Stage 5D rocprof

Profiling was not executed because the environment rejected the new Docker
request after the benchmark/control data had completed. Run
`../profile_stage5d.py` when Docker execution becomes available. Counter names
must be selected from the generated `available_counters.txt`.
