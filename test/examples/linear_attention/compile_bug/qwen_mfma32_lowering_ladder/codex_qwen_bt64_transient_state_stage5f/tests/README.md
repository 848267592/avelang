# Stage 5F Tests

Stage 5F adds no kernel or production import. The minimal regression smoke is
the existing Stage 5E direct-common-out suite; its fresh result is recorded in
`../pytest_results.txt`.
